# Snappy MVP 기획서
> 하나의 컨텐츠로 여러 SNS를, 한 번에  
> Flutter MVP 기획 문서 v1.0

---

## 목차
1. [제품 정의](#1-제품-정의)
2. [핵심 결정 사항](#2-핵심-결정-사항)
3. [사용자 흐름](#3-사용자-흐름)
4. [화면 설계](#4-화면-설계)
5. [서비스 구조](#5-서비스-구조)
6. [기술 스택](#6-기술-스택)
7. [LLM 라우터 설계](#7-llm-라우터-설계)
8. [데이터 모델](#8-데이터-모델)
9. [프롬프트 시스템](#9-프롬프트-시스템)
10. [MVP 일정](#10-mvp-일정)
11. [리스크 및 대응](#11-리스크-및-대응)
12. [요약](#12-요약)

---

## 1. 제품 정의

### 1.1 한 줄 정의

> 원본 컨텐츠 하나를 입력하면, 사용자가 직접 정의한 여러 출력 매체의 성격에 맞게 결과를 한 번에 생성하는 Flutter 앱

### 1.2 해결하려는 문제

컨텐츠 크리에이터, 마케터, 1인 사업자는 같은 내용을 여러 채널과 포맷에 맞게 다시 써야 하는 일이 많습니다.  
그러나 각 매체는 목적, 길이, 출력 형식, 문체 규칙이 달라서 같은 글을 그대로 복사하면 어떤 매체에서도 효과가 나오지 않습니다.

- **Instagram** — 짧고 강렬한 캡션, 해시태그, 이모지 중심
- **블로그** — 긴 본문, 검색 최적화, 정보성 문체
- **뉴스레터 / 커뮤니티 / 쇼츠 대본** — 각기 다른 구조와 CTA 필요

매번 손으로 다시 쓰는 건 시간이 많이 걸리고, ChatGPT 같은 도구를 써도 매번 프롬프트를 다시 입력해야 합니다.  
Snappy는 유저가 만든 매체 정의를 재사용해 이 반복 작업을 한 번의 입력으로 끝냅니다.

### 1.3 타깃 유저

| 타깃 | 특성 |
|---|---|
| 1차 | 부업 / 사이드 프로젝트로 SNS를 운영하는 1인 크리에이터 |
| 2차 | 소상공인 — 음식점, 카페, 공방 등을 운영하며 SNS 마케팅을 직접 하는 사람 |
| 3차 | 프리랜서 마케터 — 여러 클라이언트를 위한 컨텐츠 제작자 |

**공통 특성**
- AI 도구에 거부감 없음 (ChatGPT, Claude 사용 경험 있음)
- API 키 발급은 약간 어렵지만 가이드만 있으면 가능한 수준
- 모바일과 브라우저에서 즉석으로 컨텐츠를 만드는 일이 많음
- 월 5~30개 정도의 컨텐츠를 여러 매체에 맞게 재가공함

### 1.4 가치 제안

| 기존 방식 | Snappy 사용 시 |
|---|---|
| 매체별로 직접 글 다시 작성 | 한 번 입력 → 선택한 매체별 결과 동시 생성 |
| ChatGPT에 매번 프롬프트 입력 | 매체별 출력 규칙과 LLM 스킬을 저장해 재사용 |
| 특정 기기 앞에서 작업해야 함 | 웹과 폰에서 즉석 변환 |
| 매체별 30분 — 30분 — 30분 | 선택한 매체 결과를 5분 이내 생성 |

---

## 2. 핵심 결정 사항

### 2.1 결정 요약

| 항목 | 결정 | 근거 |
|---|---|---|
| 플랫폼 | Flutter (Web + iOS + Android) | 단일 코드베이스, 웹으로 빠른 배포 후 모바일 확장 |
| 웹 출시 | MVP에 포함 | 설치 없이 BYOK 흐름과 변환 가치를 빠르게 검증 |
| 백엔드 | 없음 (서버리스) | BYOK 모델로 서버 불필요 |
| 로그인 | 없음 | 진입 장벽 최소화, 기기/브라우저 단독 동작 |
| API 키 | BYOK — 유저 발급 키 사용 | 운영비 0원으로 검증 가능 |
| LLM | MVP는 Claude 단독, 라우터 구조로 확장성 확보 | 출력 품질 우수, 한국어 강함 |
| 출력 매체 | 유저가 직접 생성 | 고정 플랫폼보다 실제 사용 채널에 맞춤 |
| 매체 정의 | 매체 이름 + 출력 형식 + LLM 스킬 | 재사용 가능한 변환 단위 |
| 기본 프리셋 | 인스타그램, 블로그, 뉴스레터 등 예시 제공 | 입문자는 프리셋으로 시작, 숙련자는 커스터마이징 |

### 2.2 의도적으로 뺀 것들

MVP 단계에서 다음 기능들은 의도적으로 제외합니다.

- ❌ **회원 가입 / 로그인** — 기기/브라우저 단독으로 모든 기능 동작
- ❌ **결제 / 구독** — Free + BYOK 모델로 시작, 검증 후 Pro 플랜 도입
- ❌ **히스토리 저장** — 마지막 결과만 임시 보관, 영구 저장은 Phase 2
- ❌ **매체 자동 발행** — 결과를 클립보드 복사로 처리, 직접 게시
- ❌ **이미지 자동 편집 / 리사이징** — 업로드한 그대로 LLM 분석에만 활용
- ❌ **팀 / 협업 기능** — 1인 사용 우선, 팀 기능은 Phase 3
- ❌ **분석 / 통계** — 실제 게시 후 성과는 외부 도구로 확인

---

## 3. 사용자 흐름

### 3.1 첫 사용 흐름 (온보딩)

| 단계 | 화면 | 내용 |
|---|---|---|
| 1 | 환영 화면 | 최초 1회만 표시, 3장 슬라이드 |
| 2 | API 키 안내 | "Snappy를 사용하려면 Anthropic API 키가 필요합니다" + 발급 방법 가이드 링크 |
| 3 | 키 입력 | API 키 붙여넣기 → 유효성 검증 → 기기/브라우저 로컬 보안 저장소에 보관 |
| 4 | 기본 매체 확인 | 인스타그램, 블로그, 뉴스레터 등 프리셋 확인 또는 새 매체 생성 |
| 5 | 메인 화면 진입 | 바로 컨텐츠 입력 가능, 첫 변환을 위한 가이드 툴팁 |

### 3.2 일반 사용 흐름

| 단계 | 액션 | 내용 |
|---|---|---|
| 1 | 앱 실행 | 메인 화면 즉시 표시 (로그인 없음) |
| 2 | 원본 입력 | 텍스트 입력 + 이미지 첨부 (선택, 최대 5장) |
| 3 | 옵션 선택 | 대상 매체 선택 |
| 4 | 변환 시작 | [변환] 버튼 → 선택한 매체 병렬 호출 → 진행 표시 |
| 5 | 결과 확인 | 매체별 탭으로 결과 표시 |
| 6 | 복사 / 재생성 | 탭마다 [복사] 버튼 + [다시 생성] 버튼 |
| 7 | 외부 앱 이동 | 복사 후 각 매체에 직접 게시 |

### 3.3 핵심 사용 시나리오

#### 시나리오 A: 카페 사장님이 신메뉴 사진을 올릴 때
1. 카페에서 신메뉴 사진을 찍음
2. Snappy에 "인스타그램", "블로그", "동네 커뮤니티" 매체를 미리 만들어둠
3. Snappy 열어 사진 첨부 + "오늘 출시한 망고 라떼, 시즌 한정" 입력
4. [변환] 누르고 5초 대기
5. 인스타그램 매체 탭에서 결과 확인 → 복사 → 인스타에 게시
6. 블로그 매체 탭으로 이동 → 결과 복사 → 블로그 앱에서 발행

#### 시나리오 B: 1인 IT 블로거가 새 글을 쓸 때
1. "카드뉴스 캡션", "기술 블로그", "뉴스레터 요약" 매체를 생성
2. "VS Code 단축키 정리" 긴 글을 Snappy에 복붙
3. 카드뉴스 캡션 결과 → SNS 게시글로 활용
4. 기술 블로그 결과 → 검색 노출용 글로 발행
5. 뉴스레터 요약 결과 → 구독자 메일 초안으로 활용

---

## 4. 화면 설계

### 4.1 화면 목록

| # | 화면명 | 역할 |
|---|---|---|
| S1 | 환영 화면 | 최초 1회만 표시, 3장 슬라이드 |
| S2 | API 키 입력 화면 | 키 발급 가이드 + 입력 + 검증 |
| S3 | 메인 화면 (입력) | 원본 컨텐츠 입력, 매체 선택, 변환 시작 |
| S4 | 결과 화면 | 매체별 탭, 복사, 재생성 |
| S5 | 매체 목록 화면 | 생성된 매체 목록, 새 매체 만들기, 편집/삭제 진입 |
| S6 | 매체 생성 / 편집 화면 | 매체 이름, 출력 형식, LLM 스킬 입력 |
| S7 | 설정 화면 | API 키 변경, 매체 관리, 앱 정보 |

### 4.2 S3 — 메인 화면 (가장 중요)

```
┌─────────────────────────────────┐
│  Snappy              [⚙ 설정]    │  ← 상단 바
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────────┐│
│  │ 원본 텍스트를 입력하세요...    ││  ← 텍스트 입력
│  │                             ││
│  └─────────────────────────────┘│
│                                 │
│  [📷 사진 추가]  (0/5)            │  ← 이미지 첨부
│  ┌──┐ ┌──┐                       │
│  │  │ │  │                       │
│  └──┘ └──┘                       │
│                                 │
│  매체                            │
│  ☑ 인스타그램  ☑ 블로그  ☑ 뉴스레터 │
│                                 │
│  ┌─────────────────────────────┐│
│  │      ✨ 변환하기              ││  ← 메인 CTA
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

**핵심 인터랙션**
- 텍스트 입력란은 자동 확장 (최대 화면 60%까지)
- 이미지는 길게 누르면 삭제 옵션, 탭하면 확대 미리보기
- 매체 체크박스는 기본 매체를 선택, 최소 1개는 선택돼야 함
- 변환 버튼은 텍스트 비어있으면 비활성화

### 4.3 S4 — 결과 화면

```
┌─────────────────────────────────┐
│  [←]   결과            [🔄 전체]  │
├─────────────────────────────────┤
│ 인스타그램 │ 블로그 │ 뉴스레터      │  ← 유저 매체 탭
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────────┐│
│  │ 변환된 캡션 텍스트...           ││
│  │                             ││
│  │ #해시태그1 #해시태그2          ││
│  └─────────────────────────────┘│
│                                 │
│  [📋 복사]  [🔄 다시생성]  [✏ 편집]│
│                                 │
│  💡 매체 가이드                   │
│  - 저장된 LLM 스킬 기준으로 생성됨 │
│                                 │
└─────────────────────────────────┘
```

**탭별 차이**
- 각 탭은 유저가 만든 매체 이름을 사용합니다.
- 매체별 출력 형식에 따라 마크다운, 해시태그, 키워드, CTA, 대본 등 결과 구조가 달라집니다.
- 매체별 LLM 스킬은 결과 생성 시 시스템 지시문으로 사용됩니다.

### 4.4 S5 — 매체 목록 화면

```
┌─────────────────────────────────┐
│  [←]   매체 관리          [+]     │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────────┐│
│  │ 인스타그램                    ││
│  │ 캡션 본문 + 해시태그 20개       ││
│  │ [기본]                 [편집] ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 블로그                       ││
│  │ 제목 + 긴 본문 + 검색 키워드    ││
│  │ [기본]                 [편집] ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 뉴스레터                      ││
│  │ 제목 + 도입부 + 본문 + CTA     ││
│  │ [사용자]          [편집] [삭제]││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │        + 새 매체 만들기        ││
│  └─────────────────────────────┘│
│                                 │
└─────────────────────────────────┘
```

**진입 경로**
- 메인 화면의 매체 영역에서 `매체 관리` 버튼을 누르면 진입합니다.
- 설정 화면의 `매체 관리` 메뉴에서도 진입합니다.
- 매체가 0개일 수 없으므로 최초 실행 시 기본 프리셋을 자동 생성합니다.

**핵심 인터랙션**
- `+` 또는 `새 매체 만들기`를 누르면 S6 매체 생성 화면으로 이동합니다.
- 매체 카드를 누르면 상세/편집 화면으로 이동합니다.
- 기본 프리셋은 삭제 버튼을 숨깁니다.
- 유저가 만든 매체는 삭제할 수 있습니다.
- 삭제 후 현재 선택된 매체가 사라지면 남은 매체 중 첫 번째를 자동 선택합니다.

### 4.5 S6 — 매체 생성 / 편집 화면

```
┌─────────────────────────────────┐
│  [←]   매체 만들기        [저장]  │
├─────────────────────────────────┤
│                                 │
│  매체 이름                       │
│  ┌─────────────────────────────┐│
│  │ 예: 인스타그램, 뉴스레터       ││
│  └─────────────────────────────┘│
│                                 │
│  출력 형식                       │
│  ┌─────────────────────────────┐│
│  │ 예: 제목 + 본문 + CTA         ││
│  │                             ││
│  └─────────────────────────────┘│
│                                 │
│  LLM 스킬                        │
│  ┌─────────────────────────────┐│
│  │ 이 매체에 맞게 쓰는 규칙...     ││
│  │ - 첫 줄은 질문으로 시작        ││
│  │ - 이모지는 3개 이하           ││
│  │ - 마지막에 CTA 포함           ││
│  └─────────────────────────────┘│
│                                 │
│  [프리셋에서 시작] [삭제]          │
│                                 │
└─────────────────────────────────┘
```

**필드 정의**

| 필드 | 필수 | 설명 |
|---|---|---|
| 매체 이름 | 필수 | 메인 화면 선택 목록과 결과 탭에 표시되는 이름 |
| 출력 형식 | 필수 | 결과가 따라야 하는 구조, 섹션, Markdown 여부, 태그/CTA 포함 여부 |
| LLM 스킬 | 필수 | 문체, 금지 사항, 최적화 기준, 반복 사용되는 프롬프트 규칙 |

**핵심 인터랙션**
- 새 매체 만들기는 S5 매체 목록 화면에서 진입합니다.
- 기본 프리셋은 복제해서 수정할 수 있습니다.
- 기본 프리셋 원본은 삭제하지 않고, 유저가 복제한 매체는 삭제할 수 있습니다.
- 저장 후 즉시 메인 화면의 매체 선택 목록에 표시됩니다.
- 현재 선택된 매체를 삭제하면 선택 목록에서 제거하고, 남은 매체 중 하나를 자동 선택합니다.
- 매체는 최소 1개 이상 존재해야 합니다.

**검증 규칙**

| 조건 | 처리 |
|---|---|
| 매체 이름이 비어 있음 | 저장 비활성화, "매체 이름을 입력하세요" 표시 |
| 출력 형식이 비어 있음 | 저장 비활성화, "출력 형식을 입력하세요" 표시 |
| LLM 스킬이 비어 있음 | 저장 비활성화, "LLM 스킬을 입력하세요" 표시 |
| 같은 이름의 매체가 이미 있음 | 저장 가능하되 이름 옆에 "중복 이름" 경고 표시 |
| 전체 매체가 0개가 되는 삭제 | 삭제 차단 |

### 4.6 화면 이동 구조

```text
S3 메인 화면
  ├─ 매체 선택
  ├─ 매체 관리 버튼 -> S5 매체 목록 화면
  └─ 변환하기 -> S4 결과 화면

S5 매체 목록 화면
  ├─ 새 매체 만들기 -> S6 매체 생성 화면
  ├─ 매체 편집 -> S6 매체 편집 화면
  └─ 뒤로 -> S3 메인 화면 또는 S7 설정 화면

S6 매체 생성 / 편집 화면
  ├─ 저장 -> S5 매체 목록 화면
  ├─ 삭제 -> S5 매체 목록 화면
  └─ 뒤로 -> S5 매체 목록 화면

S7 설정 화면
  └─ 매체 관리 -> S5 매체 목록 화면
```

---

## 5. 서비스 구조

```text
Snappy
Flutter App: Web / iOS / Android
No Backend, No Login, BYOK

+------------------------------------------------------+
|              Flutter App: Web / iOS / Android         |
|------------------------------------------------------|
|                                                      |
|  +-------------------+                               |
|  | Onboarding        |                               |
|  | - welcome         |                               |
|  | - API key guide   |                               |
|  +---------+---------+                               |
|            |                                         |
|            v                                         |
|  +-------------------+        +-------------------+  |
|  | API Key Screen    |------->| Secure Storage    |  |
|  | - key input       |        | - Anthropic key   |  |
|  | - validation      |        +-------------------+  |
|  +---------+---------+                               |
|            |                                         |
|            v                                         |
|  +-------------------+        +-------------------+  |
|  | Home Screen       |------->| SharedPreferences |  |
|  | - text input      |        | - model           |  |
|  | - image attach    |        | - output media    |  |
|  | - media select    |        | - media skills    |  |
|  | - media select    |        +-------------------+  |
|  +---------+---------+                               |
|            |                                         |
|            v                                         |
|  +-------------------+                               |
|  | Conversion Request|                               |
|  | - original text   |                               |
|  | - images          |                               |
|  | - target media    |                               |
|  +---------+---------+                               |
|            |                                         |
+------------|-----------------------------------------+
             |
             v
+------------------------------------------------------+
|                 Conversion Pipeline                  |
|------------------------------------------------------|
|                                                      |
|  +-------------------+                               |
|  | Prompt Builder    |                               |
|  | - media name      |                               |
|  | - output format   |                               |
|  | - LLM skill       |                               |
|  | - input content   |                               |
|  +---------+---------+                               |
|            |                                         |
|            v                                         |
|  +-------------------+                               |
|  | LLM Router        |                               |
|  | - common interface|                               |
|  | - provider switch |                               |
|  +----+---------+----+                               |
|       |         |                                    |
|       v         v                                    |
|  +---------+  +---------+  +---------+               |
|  | Claude  |  | GPT     |  | Gemini  |               |
|  | MVP     |  | Phase 2 |  | Phase 2 |               |
|  +----+----+  +---------+  +---------+               |
|       |                                              |
+-------|----------------------------------------------+
        |
        v
+------------------------------------------------------+
|                    Generated Output                  |
|------------------------------------------------------|
|                                                      |
|  +-------------------+                               |
|  | Output Media A    |                               |
|  | - custom format   |                               |
|  | - generated text  |                               |
|  +-------------------+                               |
|                                                      |
|  +-------------------+                               |
|  | Output Media B    |                               |
|  | - custom format   |                               |
|  | - generated text  |                               |
|  +-------------------+                               |
|                                                      |
|  +-------------------+                               |
|  | Output Media C    |                               |
|  | - custom format   |                               |
|  | - generated text  |                               |
|  +-------------------+                               |
|                                                      |
+----------------------+-------------------------------+
                       |
                       v
+------------------------------------------------------+
|                    Result Screen                     |
|------------------------------------------------------|
| - platform tabs                                      |
| - copy button                                        |
| - regenerate button                                  |
| - partial failure retry                              |
+----------------------+-------------------------------+
                       |
                       v
+------------------------------------------------------+
|                         RAM                          |
|------------------------------------------------------|
| - latest ConversionResult only                       |
| - no permanent history in MVP                        |
+------------------------------------------------------+
```

---

## 6. 기술 스택

### 6.1 핵심 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Flutter (최신 안정 버전) | Web + iOS + Android 단일 코드 |
| 언어 | Dart 3 | Flutter 표준 |
| 상태 관리 | Riverpod | 타입 안전, 테스트 용이, 유지보수 좋음 |
| HTTP 클라이언트 | dio | 인터셉터, 타임아웃 관리 편함 |
| 라우팅 | go_router | 선언적 라우팅, 딥링크 대응 |

### 6.2 주요 패키지

| 용도 | 패키지 |
|---|---|
| API 키 보안 저장 | flutter_secure_storage (Web Crypto 기반 웹 저장 + iOS Keychain + Android Keystore) |
| 일반 설정 저장 | shared_preferences |
| 이미지 선택 | image_picker |
| 이미지 압축 | flutter_image_compress (LLM 전송 전 용량 줄이기) |
| 마크다운 렌더링 | flutter_markdown (매체 출력 형식이 Markdown일 때 미리보기용) |
| 클립보드 | flutter/services (내장) |
| 외부 링크 열기 | url_launcher (API 키 발급 페이지로 이동) |
| 로깅 | logger |

### 6.3 프로젝트 구조

```
snappy/
├── lib/
│   ├── main.dart
│   ├── app/
│   │   ├── app.dart                # MaterialApp 진입
│   │   ├── router.dart             # go_router 설정
│   │   └── theme.dart              # 디자인 시스템
│   ├── core/
│   │   ├── llm/
│   │   │   ├── llm_provider.dart   # 추상 인터페이스
│   │   │   ├── anthropic.dart      # Claude 구현
│   │   │   ├── openai.dart         # (Phase 2)
│   │   │   └── google.dart         # (Phase 2)
│   │   ├── storage/
│   │   │   ├── secure_storage.dart # API 키
│   │   │   └── prefs_storage.dart  # 일반 설정
│   │   └── prompts/
│   │       ├── prompt_builder.dart # 매체 정의 기반 프롬프트 조합
│   │       └── media_presets.dart  # 기본 매체 프리셋
│   ├── media/
│   │   ├── output_media.dart       # 매체 정의 모델
│   │   └── media_repository.dart   # 매체 저장/조회
│   ├── features/
│   │   ├── onboarding/
│   │   ├── home/                   # 메인 입력 화면
│   │   ├── result/                 # 결과 화면
│   │   ├── media_list/
│   │   ├── media_editor/
│   │   └── settings/
│   └── shared/
│       ├── widgets/
│       └── models/
├── assets/
│   └── images/
├── web/                         # Flutter Web 배포 셸
│   ├── index.html
│   └── manifest.json
├── android/                     # Android 플랫폼 셸
├── ios/                         # iOS 플랫폼 셸
├── pubspec.yaml
└── README.md
```

---

## 7. LLM 라우터 설계

### 7.1 추상 인터페이스

멀티 LLM 확장을 대비해 모든 프로바이더가 따르는 공통 인터페이스를 정의합니다.  
MVP에서는 Claude만 구현하지만 구조는 미리 잡아둡니다.

```dart
abstract class LLMProvider {
  String get name;
  String get defaultModel;

  Future<String> generate({
    required String apiKey,
    required String systemPrompt,
    required String userContent,
    List<ImageData>? images,
    String? model,
  });

  Future<bool> validateApiKey(String apiKey);
}
```

### 7.2 Claude 구현

```dart
class AnthropicProvider extends LLMProvider {
  @override String get name => 'Anthropic';
  @override String get defaultModel => 'claude-sonnet-4-6';

  final Dio _dio = Dio();

  @override
  Future<String> generate({
    required String apiKey,
    required String systemPrompt,
    required String userContent,
    List<ImageData>? images,
    String? model,
  }) async {
    final response = await _dio.post(
      'https://api.anthropic.com/v1/messages',
      options: Options(headers: {
        'x-api-key': apiKey,
        'anthropic-version': '2023-06-01',
        'content-type': 'application/json',
      }),
      data: {
        'model': model ?? defaultModel,
        'max_tokens': 4096,
        'system': systemPrompt,
        'messages': _buildMessages(userContent, images),
      },
    );
    return response.data['content'][0]['text'];
  }
}
```

### 7.3 호출 흐름

| 순서 | 동작 | 내용 |
|---|---|---|
| 1 | API 키 로드 | flutter_secure_storage에서 키 읽기 |
| 2 | 매체 정의 로드 | SharedPreferences에서 매체 이름, 출력 형식, LLM 스킬 읽기 |
| 3 | 이미지 처리 | 이미지가 있다면 base64 인코딩 + 압축 |
| 4 | 병렬 호출 | Future.wait로 선택된 매체 동시 호출 |
| 5 | 결과 처리 | 성공/실패 각각 처리, UI에 결과 또는 재시도 표시 |

### 7.4 에러 처리

| 에러 종류 | 처리 방법 |
|---|---|
| API 키 무효 (401) | "키가 잘못됐어요" 안내 + 설정 화면으로 이동 버튼 |
| 크레딧 부족 (402) | "크레딧이 부족해요" + 충전 페이지 링크 |
| Rate Limit (429) | "잠시 후 다시 시도하세요" + 자동 재시도 (지수 백오프) |
| 네트워크 오류 | "네트워크 연결을 확인하세요" + 재시도 버튼 |
| 부분 실패 | 성공한 탭은 표시, 실패한 탭은 재시도 버튼만 |

---

## 8. 데이터 모델

### 8.1 저장 위치 분리

모든 데이터는 기기 또는 브라우저 로컬 저장소에만 존재합니다.

| 저장소 | 내용 | 이유 |
|---|---|---|
| Secure Storage / Web Storage | API 키 | 탈취 시 금전 피해 발생 가능 |
| SharedPreferences | 매체 정의, 기본 설정 | 민감하지 않음, 빠른 접근 필요 |
| 메모리 (RAM) | 최근 변환 결과 | 앱 종료 시 사라져도 무방 |

### 8.2 데이터 모델 정의

#### AppSettings (SharedPreferences)
```dart
class AppSettings {
  final LLMProviderType provider;        // 'anthropic'
  final String anthropicModel;           // 'claude-sonnet-4-20250514'
  final List<String> selectedMediaIds;
  final List<OutputMediaDefinition> mediaDefinitions;
  final bool onboardingDone;
}
```

#### OutputMediaDefinition (SharedPreferences)
```dart
class OutputMediaDefinition {
  final String id;
  final String name;          // 예: '인스타그램', '블로그', '뉴스레터'
  final String outputFormat;  // 예: '캡션 + 해시태그', 'Markdown', '메일 초안'
  final String llmSkill;      // 이 매체에 맞게 쓰도록 지시하는 LLM 스킬
  final bool isDefaultPreset;
  final DateTime createdAt;
  final DateTime updatedAt;
}
```

**저장 규칙**

| 동작 | 처리 |
|---|---|
| 생성 | UUID 기반 `id`를 발급하고 SharedPreferences의 매체 목록에 추가 |
| 수정 | 동일 `id`의 매체 정의를 갱신하고 `updatedAt` 변경 |
| 삭제 | `isDefaultPreset == false`인 매체만 삭제 가능 |
| 복제 | 프리셋 또는 기존 매체를 복사해 새 `id`와 `isDefaultPreset = false`로 저장 |
| 선택 | `selectedMediaIds`에 저장, 앱 재실행 후에도 유지 |

#### ApiKeys (Secure Storage)
```dart
// 키-값 형태로 저장
{
  'anthropic_api_key': 'sk-ant-...',
  // Phase 2:
  // 'openai_api_key': 'sk-...',
  // 'google_api_key': 'AIza...',
}
```

#### ConversionResult (메모리)
```dart
class ConversionResult {
  final String originalText;
  final List<ImageData> images;
  final Map<String, MediaResult> results;
  final DateTime createdAt;
}

class MediaResult {
  final String mediaId;
  final String mediaName;
  final String? text;          // 성공 시
  final String? error;         // 실패 시
  final Map<String, dynamic>? metadata;
}
```

---

## 9. 프롬프트 시스템

### 9.1 매체 정의 입력값

유저는 출력 매체를 직접 생성할 수 있습니다. MVP에서 매체 생성에 필요한 최소 입력은 다음 3가지입니다.

| 입력 | 설명 | 예시 |
|---|---|---|
| 매체 이름 | 결과 탭과 선택 목록에 표시되는 이름 | 인스타그램, 블로그, 뉴스레터 |
| 출력 형식 | 최종 결과가 따라야 할 구조 | 캡션 + 해시태그, Markdown, 이메일 초안 |
| LLM 스킬 | 해당 매체의 문체, 규칙, 금지 사항, 최적화 기준 | 첫 줄은 질문, 이모지 3개 이하, CTA 포함 |

### 9.2 프롬프트 조립 규칙

매체별 최종 시스템 프롬프트는 앱의 공통 지시문과 유저가 만든 매체 정의를 조합해 생성합니다.

```text
최종 시스템 프롬프트 =
  Snappy 공통 변환 지시문
  + 매체 이름
  + 출력 형식
  + LLM 스킬
  + 원본 입력 처리 규칙
```

### 9.3 최종 프롬프트 예시

```text
당신은 컨텐츠 재가공 전문가입니다.

아래 원본 컨텐츠를 지정된 매체에 맞게 변환하세요.

매체 이름:
{mediaName}

출력 형식:
{outputFormat}

LLM 스킬:
{llmSkill}

규칙:
1. 원본의 핵심 정보는 유지하세요.
2. 매체 이름과 출력 형식에 맞게 구조를 바꾸세요.
3. LLM 스킬에 적힌 규칙을 최우선으로 따르세요.
4. 없는 사실을 추가하지 마세요.
5. 결과만 출력하세요.
```

### 9.4 기본 매체 프리셋

MVP는 빈 상태로 시작하지 않도록 몇 가지 기본 프리셋을 제공합니다. 단, 이 프리셋도 일반 매체 정의와 동일하게 수정/삭제/복제할 수 있습니다.

| 매체 이름 | 출력 형식 | LLM 스킬 |
|---|---|---|
| 인스타그램 | 캡션 본문 + 해시태그 20개 | 첫 줄 후킹, 짧은 문단, 이모지 적절히 사용 |
| 블로그 | 제목 + 긴 본문 + 검색 키워드 | 정보성 구조, 소제목 3개 이상, 검색 유입 고려 |
| 뉴스레터 | 제목 + 도입부 + 본문 + CTA | 구독자에게 말하듯 작성, 마지막에 행동 유도 |

### 9.5 매체 커스터마이징 흐름

1. 설정 → 매체 관리 진입
2. 새 매체 만들기 또는 기존 매체 편집 선택
3. 매체 이름 입력
4. 출력 형식 입력
5. LLM 스킬 입력
6. 저장 후 메인 화면의 매체 선택 목록에 표시
7. 변환 시 선택된 매체 정의를 사용

---

## 10. MVP 일정

### 10.1 4주 빌드 플랜

| 주차 | 주제 | 산출물 |
|---|---|---|
| 1주차 | 매체 정의 + 라우터 | 기본 매체 프리셋, 매체 기반 프롬프트 조립, Claude API 호출 동작, CLI로 결과 검증 |
| 2주차 | 핵심 화면 | S3 메인 화면 + S4 결과 화면 + 변환 흐름 동작 |
| 3주차 | 주변 화면 | S1 환영 + S2 키 입력 + S5 매체 목록 + S6 매체 생성/편집 + S7 설정 + 이미지 업로드 |
| 4주차 | 출시 준비 | 웹 배포 + iOS TestFlight + Android 내부 테스트 + 베타 피드백 |
| 5주차+ | 출시 | 웹 공개 + App Store + Play Store 정식 출시, 피드백 기반 빠른 개선 |

### 10.2 출시 후 우선순위

1. 실사용 피드백 수집 (10명 이상 인터뷰)
2. 결과 품질 개선 — 매체 프리셋과 LLM 스킬 튜닝이 가장 임팩트 큼
3. 히스토리 기능 — 두 번째로 요청 많을 것으로 예상
4. 멀티 LLM (GPT, Gemini) — 라우터 구조는 이미 준비됨
5. 랜딩 페이지 — Framer로 마케팅 사이트 추가

---

## 11. 리스크 및 대응

### 11.1 주요 리스크

| 리스크 | 내용 | 대응 |
|---|---|---|
| API 키 발급 허들 | 일반 유저에겐 API 키 발급이 어려움 | S2에 단계별 스크린샷 가이드 + 외부 링크 |
| 출력 품질 편차 | 같은 입력이라도 결과가 매번 다름 | 재생성 버튼 제공 + 매체별 LLM 스킬 편집 옵션 |
| API 비용 인지 부족 | 유저가 변환 1회 비용을 모름 | 변환 후 "이번 사용 약 50원" 표시 |
| 웹 BYOK 보안 인식 | 브라우저에서 유저 API 키를 직접 사용하는 구조를 오해할 수 있음 | 온보딩에 로컬 저장/직접 전송 방식을 명시하고 `anthropic-dangerous-direct-browser-access` 헤더를 사용 |
| LLM 정책 변경 | 프로바이더가 갑자기 정책/가격 바꿈 | 멀티 LLM 라우터로 즉시 다른 곳으로 전환 가능 |
| 앱스토어 심사 | AI 컨텐츠 생성 앱은 가이드라인 까다로움 | 성인 컨텐츠 필터, 면책 조항, 약관 명확히 |

### 11.2 검증 지표

MVP 출시 후 4주 동안 다음 지표를 추적해 다음 단계를 결정합니다.

- **리텐션 — Day 7 / Day 30**: 30% 이상이면 가치 있음
- **변환 횟수 / 유저**: 주당 평균 5회 이상이면 진짜 사용 중
- **재생성 비율**: 30% 이하 = 결과 만족 / 50% 이상 = 프롬프트 개선 필요
- **API 키 입력 완료율**: 50% 이하 = 온보딩 개선 필요

---

## 12. 요약

> **운영비 0원 · 인프라 0개 · 코드베이스 1개 · 출시까지 4주**

### 핵심 결정
- **Flutter 단일 코드베이스** — Web + iOS + Android
- **BYOK + 로그인 없음** — 운영비 제로, 진입 장벽 최소
- **Claude로 시작, 멀티 LLM 확장 가능** — 라우터 구조 미리 잡음
- **유저 정의 매체 집중** — 매체 이름 + 출력 형식 + LLM 스킬
- **매체 편집 가능** — 입문자는 프리셋, 숙련자는 직접 커스터마이징

### 검증할 것
- 타깃 유저가 웹과 모바일에서 SNS 컨텐츠를 실제로 변환하는가?
- BYOK 허들이 출시 후 실제로 얼마나 큰가?
- 기본 매체 프리셋의 출력 품질이 직접 ChatGPT 쓰는 것보다 나은가?
- 유저 정의 매체 동시 변환의 가치가 충분히 매력적인가?

> 작게 만들고, 빠르게 검증하고, 사용자에게 답을 듣자
